services:
  sheaft:
    build:
      context: .
      dockerfile: build/Dockerfile
    image: sheaft:dev
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command:
      - run
      - --model
      - examples/outputs/model.sample.json
      - --policy
      - configs/gate.policy.example.yaml
      - --out-dir
      - examples/outputs/generated
      - --seed
      - "42"
