# Sheaft runtime config (MVP)
model: examples/outputs/model.sample.json
output_dir: out
seed: 42

simulation:
  trials: 10000
  failure_probability: 0.05

policy:
  file: configs/gate.policy.example.yaml
