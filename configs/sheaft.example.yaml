schema_version: "1.0"
listen: ":8080"

artifact:
  path: artifacts/bering
  mode: directory
  patterns:
    - "*.json"

analysis_file: configs/analysis.example.yaml

poll_interval: 30s
watch_fs: true
watch_polling: true

history:
  max_items: 20
  disk_dir: out/history
